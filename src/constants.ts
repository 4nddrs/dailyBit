// Shared character limit for free-text task-like fields: Developer
// View task/update text and Lead View assignment descriptions all cap at the
// same length so the UI stays consistent across both views.
export const TASK_DESCRIPTION_LIMIT = 500;
